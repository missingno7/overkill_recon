# Clean-world experiment

User direction authorizes a separate, bounded clean C reconstruction here.
Keep exact ASM and both conversion labs intact. No historical ABI adapters,
addresses or original executable bytes in src/. Oracle mapping belongs only
in verification scripts/metadata. Ordinary C calls compose reviewed leaves.
No game/SDL/platform framework: one history closure and its local DOS demonstrator.
