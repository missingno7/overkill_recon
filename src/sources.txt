# Sources in link order. MODULE1..3 each contribute to the MAIN and FAR0F7F
# segments; the order reproduces the original relocation order, which proves at
# least three link modules (not necessarily these exact ones). MODULE4 is the far
# page viewer: a paragraph-aligned module that reproduces the zero padding before it. The other files
# each hold one address frame.
MODULE1.ASM
MODULE2.ASM
MODULE3.ASM
MODULE4.ASM
SLOT1022.ASM
SEG1534.ASM
SEG153A.ASM
DATA.ASM
